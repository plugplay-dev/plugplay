import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const Color primary = Colors.amber;
  static const Color primaryAccent = Colors.amberAccent;

  // Backgrounds
  static const Color background = Color(0xFF090909);
  static const Color surface = Color(0xFF171717);
  static const Color surfaceLight = Color(0xFF1B1B1B);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textHint = Colors.white54;

  // Icons
  static const Color iconPrimary = Colors.amber;
  static const Color iconSecondary = Colors.white70;

  // States
  static const Color liked = Colors.redAccent;
  static const Color border = Colors.white10;

  // Slider
  static const Color sliderActive = Colors.amber;
  static const Color sliderInactive = Colors.white24;
}